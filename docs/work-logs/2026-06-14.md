# 작업 로그 2026-06-14

## 모바일 반응형 후속 다듬기 + 이미지 복구 2단계

### 모바일 UI 다듬기 (프로덕션 배포 완료)
- **홈**: 모바일에서 빠져있던 카테고리 12개를 4열 타일 그리드로 추가, 프로모 배너 2열화 → "휑한 한 줄" 해소
- **헤더**: 로고 태그라인 자간 축소+한 줄 고정(깨짐 해결), 로그인/회원가입/위시리스트 패딩 축소(비율 정리)
- 모두 main push → Vercel 자동배포, 라이브 확인

### 이미지 복구 2단계 — tbuc 335개 (자격증명 없이)
- aceuniform은 원본이 http-only(mixed-content)라 R2 업로드 필수지만, **tbuc은 원본이 https**(`www.tbuc.co.kr`)라 DB를 원본으로 직결 가능
- `scripts/fix-tbuc-images-direct.ts`: 상품 코드 ↔ `tbuc-scraped.json` 매칭(335/335), 원본 HEAD 200 전수 확인 후 DB url 교체
- `next.config.ts`: `www.tbuc.co.kr` pathname 제한 해제 (원본 경로가 `/data/**`)
- 배포 후 tbuc 상품 페이지 200 + 이미지 정상 로드 확인

### 현재 이미지 상태
```
정상 6,471 / 7,815 (83%)
  R2          6,136
  tbuc.co.kr    335
깨짐 1,344
  aceuniform  1,236  ← R2 업로드 필요 (자격증명 대기)
  flat          108  ← 원본 소스 불명 (별도 조사)
```

### 남은 작업 — aceuniform 1,236개
- 원본은 `aceuniform-scraped.json`에 있고 사이트도 살아있음(http 200). 단 http라 hotlink 불가 → R2 업로드 필수
- 매핑: 상품 slug에 itId 포함(`ace-...-{itId}`) → 스크랩 자동 매칭 가능
- **블로커**: R2 자격증명. 기존 값은 터미널 export로만 써서 소실(문서 image-cdn-migration.md에 명시). Secret은 1회만 표시되므로 **새 R2 API 토큰 발급 필요**
- 발급 후 `.env.local`에 3개(CLOUDFLARE_ACCOUNT_ID/R2_ACCESS_KEY_ID/R2_SECRET_ACCESS_KEY) 추가 → 다운로드→R2 업로드→DB 교체 스크립트 실행하면 마무리

### flat 108개 → 삭제 처리 (완료)
- 정체: 유한킴벌리 안전화 18개 상품의 **상세(detail) 이미지** (메인 0개)
- 18개 상품 모두 메인 이미지는 정상 → 목록/카드 노출엔 영향 없음
- 원본 소스 어디에도 없음(복구 불가) → 깨진 detail 레코드 108개 삭제

## ✅ 이미지 최종 상태 — 100% 정상
```
전체 7,707 (108 삭제 반영)
  R2 (r2.dev)     7,372  (aceuniform 1,236 포함)
  tbuc.co.kr        335
  깨짐(supabase)      0   ← 0개!
```
- R2 자격증명: `.env.local`에 저장됨 (예전엔 터미널 export만 해서 소실됐던 것)
- 신규 스크립트: `fix-tbuc-images-direct.ts`, `fix-aceuniform-images-r2.ts`, `migrate-images-to-r2.ts`
- 모든 변경 DB/R2 기반 → 프로덕션 즉시 반영(코드 배포는 tbuc next.config만 필요했고 완료)
